#include <stdio.h>

int main()
{
    int arr[] = {2, 3, 5, 4, 5, 3, 4};
    int result = 0;
    int i;

    for(i = 0; i < 7; i++)
    {
        result ^= arr[i];
    }

    printf("Non-Repeating Element = %d\n", result);

    return 0;
}
Sample Output
Non-Repeating Element = 2
