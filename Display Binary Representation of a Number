#include <stdio.h>

int main()
{
    int num, i;

    printf("Enter Number: ");
    scanf("%d", &num);

    printf("Binary = ");

    for(i = 31; i >= 0; i--)
    {
        printf("%d", (num >> i) & 1);
    }

    return 0;
}
Sample Output
Enter Number: 10
Binary = 00000000000000000000000000001010
